<%= form_with(model: number_running, local: true) do |form| %>
  <% if number_running.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(number_running.errors.count, "error") %> prohibited this number_running from being saved:</h2>

      <ul>
      <% number_running.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
